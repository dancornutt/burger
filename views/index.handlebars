<header>
   <img src="../public/assets/img/burger.png" alt="Burger Image">
    <h1>Eat-Da-Burger</h1> 
</header>

<section>
    <h2>Burgers that are ready for consumption</h2>
    <div>
        <ul>
            {{#each burgers}}
                {{#unless devoured}}
                    {{this.burger_name}}
                    <button id="this.id", type="button">Devour it!</button>
                {{/unless}}
            {{/each}}
        </ul>
    </div>
    

    {{!-- <h2>Burgers are already eaten :(</h2>

    <ul>
    {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block}}
        {{/if}}
    {{/each}}
    </ul>

    <h2>Add a Burger!</h2>
    <form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="burgerName">
    </div>

    <div class="form-group">
        <label for="eaten">Eaten already?</label><br>
        <input type="radio" name="devoured" value="1" checked> Ready to eat!<br>
        <input type="radio" name="devoured" value="0"> Crumbs are left...
    </div>

    <button type="submit">Add Burger</button>
    </form> --}}

</section>
